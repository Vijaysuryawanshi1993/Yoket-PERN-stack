const config = {};

config.apiBasePath = "http://localhost:5000";

// endpoints
config.login = "/login";
config.signup = "/register";

config.createTask = "/postTask";
config.getAllTask = "/getTasks";

export default config;
